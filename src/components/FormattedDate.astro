---
export interface Props {
	date: Date
}

const { date } = Astro.props
---

<time
	class='text-sm dark:text-white text-black font-bold text-opacity-60'
	datetime={date.toISOString()}
>
	{
		date.toLocaleDateString('ru-RU', {
			year: 'numeric',
			month: 'long',
			day: 'numeric'
		})
	}
</time>
